extract obj_props
let spikeGroup = ?g

setup_kill = () {
    $.add(obj{
        OBJ_ID: 8,
        X: 120,
        Y: 55,
        GROUPS:[1g, 2g, spikeGroup] 
    })
}

kill = (spikeG: @group = spikeGroup){
    -> spikeG.move(-120, 0, 0.1)
}

return {
    setup_kill,
    kill
}